// components/three/LabScene.tsx
import { GroupProps } from '@react-three/fiber';

const LabScene = (props: GroupProps) => {
    return (
        <group {...props}>
            {/* Your lab scene components */}
        </group>
    );
};

export default LabScene;